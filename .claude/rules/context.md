# Context Rules

- Keep consistency across the existing project structure (naming conventions, file organization, patterns)
- Run integration tests after any modification to verify nothing is broken
- After any modification to source code, update CLAUDE.md and any relevant context files (e.g. `domain/context.md`, `infrastructure/context.md`) to reflect the current state
